# Jellycat Progress

## 2026-05-06 UTC

- Started planning-with-files workflow for Jellycat multi-language analysis and cleanup.
- Read skill instructions and ran session catchup script.
- Confirmed no existing planning files were visible in the current `dataset` directory.
- Created `task_plan.md`, `findings.md`, and `progress.md`.
- Current focus: Phase 1 repository/workflow discovery.
- User clarified that unclear requirements and confirmation-needed decisions must be asked directly; do not decide unilaterally.
- Asked the user 10 confirmation questions covering language scope, analysis output path/formats, reject semantics, VAD policy, split ID policy, output promotion, stage7 inputs, and prefix/suffix schema.
- Marked Phase 1 as blocked in `task_plan.md` pending those answers; no implementation changes should proceed until confirmation.
- Recorded user answers: future arbitrary language support, current ZH/EN only, `dataset/analysis` output, PNG plots, Markdown reports, three impact scenarios, natural VAD spans, network install may be needed, versioned outputs first, and stage7-before fbank path.
- Reopened Phase 1 as in progress; remaining blockers are split child ID/path policy, prefix/suffix schema, and VAD dependency install choice/approval.
- Inspected EN official summary and confirmed EN has merged manifests and podcast-level JSONLs.
- Inspected stage7-before ZH fbank path and sampled `jellycat_zh_cuts_train_raw.jsonl.gz`; rows are Lhotse `MonoCut` JSON objects with nested supervision and recording.
- Checked VAD-related Python packages: `torch`, `torchaudio`, `onnxruntime`, `soundfile` installed; `silero_vad` and `webrtcvad` not installed.
- Marked Phase 1 complete in `task_plan.md`; next phase is blocked only on policy/schema confirmations, not repository discovery.
- Planning hook requested continued work; reread `task_plan.md`, `findings.md`, and `progress.md`.
- Checked plotting dependencies: `numpy` is installed but `matplotlib` is not installed.
- Started Phase 2 and created `analysis/recipe/`; will use a pure-Python PNG writer to avoid adding dependencies for the statistics step.
- Added `analysis/recipe/analyze_jellycat_language_stats.py`, a generic multi-language stats script with default ZH/EN manifests, Markdown output, PNG charts, and optional post-policy manifest comparison.
- Smoke-tested the stats script on 1,000 records per language under `/tmp/jellycat_analysis_smoke`; it completed and wrote Markdown/PNG outputs.
- User confirmed prefix/suffix schema is acceptable and requested `text` inside each context object.
- Investigated `_V0001` split suffix compatibility: active pipeline treats ids as strings, but Jellycat sample validators and docs have strict `_W\\d+` examples/regexes that must be updated.
- User confirmed `_V0001` naming is acceptable; only strict validation scripts and documentation examples need correction.
- Started full ZH/EN analysis run with `python3 analysis/recipe/analyze_jellycat_language_stats.py --output-dir analysis`; it is still running.
- Full ZH/EN analysis completed and wrote `analysis/jellycat_language_stats.md` plus eight PNG plots under `analysis/`.
- Verified all generated PNG files with Pillow.
- Added `--progress-interval` to `analysis/recipe/analyze_jellycat_language_stats.py` and smoke-tested it on 1,000 records per language.
- Marked Phase 2 complete in `task_plan.md`.
- Updated Jellycat sample validators to accept optional `_Vdddd` suffixes on utterance FLAC names.
- Updated Jellycat ZH/EN full README files, ZH data report, and README generator to document VAD child naming under the same original `W`.
- Re-ran ZH and EN sample validators successfully, and confirmed regex accepts `_V0001` examples.
- Confirmed `torchaudio.functional.vad` is available locally; `silero_vad` is still unavailable unless installed.
- Created `analysis/jellycat_vad_cleanup_plan.md` with the confirmed cleanup policy, split naming, versioned output layout, rewrite surfaces, context schema, and validation plan.
- Added `Jellycat/prepare_data/generate_jellycat_vad_policy_lists.py` for language-generic direct reject and VAD candidate list generation.
- Initial smoke test lacked a max-records cap and began a full scan; stopped PID `1333873`, added `--max-records`, then reran a 1,000-record smoke successfully.
- Added `Jellycat/prepare_data/add_jellycat_context_fields.py` for prefix/suffix context enrichment from podcast-level JSONLs.
- Smoke-tested context enrichment on two ZH podcast files under `/tmp/jellycat_context_smoke`; wrote 78,700 enriched records and verified context fields include `text`, far flags, and language-prefixed wavs in the merged segment output.
- Added generic `Jellycat/prepare_data/prepare_jellycat.py` based on the shared EN/ZH implementation.
- Moved old EN/ZH implementations to `_legacy.py` files and replaced `prepare_jellycat_zh.py` / `prepare_jellycat_en.py` with thin wrappers around the generic script.
- Smoke-tested tiny ZH and EN generic prepare outputs under `/tmp` and validated them with existing Jellycat sample validators.
- Added the long-audio screening and VAD split policy to the README generator, ZH/EN full READMEs, and the ZH data report.
- Fixed stale ZH reproduction commands in `Jellycat/readme/Jellycat_EN_full_dataset_readme.md`.
- User narrowed the immediate scope: only correct strict validation scripts and documentation examples for `_V0001` child naming; pause further VAD/rewrite implementation work.
- Updated README path examples and the README generator so examples show both the original `W` FLAC and optional `_V0001` child FLAC under the same `W`.
- Ran syntax checks for the two validators and README generator.
- Re-ran ZH and EN sample validators successfully; regex smoke confirmed both strict validators accept representative `_V0001` child FLAC paths.
- User requested the active cleanup policy: reject all `>60s`; VAD-split `30-60s`; reject post-VAD children still `>30s`; do not touch actual audio, only manifests/reject lists.
- Added `Jellycat/prepare_data/generate_jellycat_manifest_vad_policy.py`, which writes non-colliding `duration60_vad30_manifest_only_v1` outputs: parent rejects, child rejects, all rejects, VAD split map, flat annotation segments, optional VAD candidates, optional manifest-only preview, and summary.
- User clarified not to split label text now. The VAD child rows now keep child `text` empty, retain `parent_text` / `source_text`, and mark `needs_text_annotation=true` for later ASR annotation.
- Smoke-tested the manifest-only VAD policy generator on the first 1,000 ZH records under `/tmp`; it produced 570 kept VAD children, 8 post-VAD child rejects, and 578 flat annotation rows with source JSONL indexes and source text.
- Planning hook requested continuation. Re-read `task_plan.md`, `progress.md`, and the planning-with-files skill instructions. Full post-VAD reject generation remains blocked on explicit VAD backend choice; safe continuation is to produce clearly named pre-VAD classify-only outputs that do not claim to be the final post-VAD reject set.
- Adjusted `generate_jellycat_manifest_vad_policy.py` so `--classify-only` writes only parent reject, pre-VAD all reject, VAD candidates, and summary; it no longer creates empty child reject / split map / annotation outputs.
- Smoke-tested classify-only mode on the first 1,000 ZH records under `/tmp`; output naming is `duration60_vad30_manifest_only_v1_prevad_classify_only` to avoid confusion with final post-VAD lists.
- Generated full ZH pre-VAD classify-only outputs under `/inspire/qb-ilm/project/embodied-multimodality/chenxie-25019/zhikang/Jellycat/manifests/ZH/duration60_vad30_manifest_only_v1_prevad_classify_only/`: 26,697,838 records seen, 1,787 `>60s` parent rejects, 3,132,739 `30-60s` VAD candidates.
- Fixed classify-only progress logging and generated full EN pre-VAD classify-only outputs under `/inspire/qb-ilm/project/embodied-multimodality/chenxie-25019/zhikang/Jellycat/manifests/EN/duration60_vad30_manifest_only_v1_prevad_classify_only/`: 25,066,601 records seen, 239 `>60s` parent rejects, 3,307,843 `30-60s` VAD candidates.
- Verified ZH and EN parent/all reject JSONL line counts match the summaries. Existing and new manifest files in the target tree are owned by `root:root`.
- User approved Silero VAD and clarified the authoritative cut source: VAD returns timestamps, but backfill/cutting must cut child audio from the `raw_data` source episode using `source_wav + source_start_time + vad offsets`, not from already cut W FLACs.
- Direct `pip install silero-vad` was unavailable from the internal index and GitHub package install was blocked by missing `hatchling`; deployed Silero as a read-only source checkout under `Jellycat/prepare_data/external/silero-vad/` and verified direct import/model load.
- Updated `Jellycat/prepare_data/generate_jellycat_manifest_vad_policy.py` with Silero backend support and raw-cut metadata fields in split/annotation outputs.
- Smoke-tested Silero VAD policy generation on 1,000 ZH rows: 128 VAD candidates, 628 kept children, 12 child rejects, 5 parents with no kept child; kept child duration was about 1.120h from 1.295h candidate audio.
- Added `Jellycat/prepare_data/cut_jellycat_vad_annotation_audio.py` to materialize kept child FLACs from raw source audio and write an ASR audio-list file; smoke-tested 10 cuts and verified 24 kHz mono child FLAC durations.
- Added `Jellycat/prepare_data/backfill_jellycat_vad_asr_results.py` to replace split parent rows with ASR-filled child rows using VibeVoice sidecar JSONs, and to optionally update Lhotse recordings/supervisions outputs.
- Added `Jellycat/prepare_data/transcribe_jellycat_vad_audio_list_with_vibevoice.py` as a dataset-side wrapper that calls VibeVoice API helpers from the external repo without modifying that repo.
- Smoke-tested backfill with fake VibeVoice sidecars on the first split parent: wrote 5 child rows, preserved raw source metadata, and filled child `text` from sidecar ASR JSON.
- Ran `py_compile` across the modified Jellycat scripts; all passed.
- Verified `--help` for the raw-source cut script, ASR backfill script, VibeVoice audio-list wrapper, and Silero VAD policy generator.
- Changed the VibeVoice wrapper to use worker processes instead of threads so stdout redirection inside VibeVoice helper calls is process-local and does not corrupt concurrent output.
- Added `sys.dont_write_bytecode = True` before importing the external VibeVoice helper so future wrapper runs do not create Python bytecode files under the VibeVoice checkout.
- Re-smoke-tested the VibeVoice wrapper with an empty audio list; it imported the external VibeVoice helper and wrote a zero-record summary.
- Moved the VibeVoice worker entrypoint to module scope and re-ran py_compile plus the empty audio-list smoke successfully.
- Final py_compile across the Jellycat analysis/VAD/context/prepare/backfill script set passed.
- Adversarial review found that backfill-generated supervisions were too selective in `custom`; changed them to preserve all non-standard segment metadata, including raw-cut, context, VAD, and ASR fields.
- Re-ran backfill smoke with segment/recording/supervision outputs; it wrote 5 child rows and verified a child supervision custom contains `raw_cut_*`, `vad_parent_id`, `parent_text`, and `asr_json`.
- Final post-review py_compile across the modified script set passed.
- Stop hook requested continuation after the previous handoff. Current status: full Silero VAD/ASR production run is still intentionally not launched pending shard/concurrency confirmation; remaining work is to complete the stage7-before update design/script and validation checklist.
- User questioned the `6,440,582` / `65,537h` VAD candidate estimate. Recomputed from ZH/EN pre-VAD classify-only summaries: candidate count is confirmed for `30s < duration <= 60s`, but it is a pre-VAD candidate workload, not final kept child ASR duration.
- Clarified to user that `>60s` has not been removed from official manifests/audio. It is only listed in versioned pre-VAD parent reject JSONLs so far: ZH `1,787` rows / `136.68h`, EN `239` rows / `41.58h`, total `2,026` rows / `178.26h`. The `65,537h` VAD workload already excludes `>60s`.
- User pointed out `analysis/ZH_duration_distribution.png` and `analysis/EN_duration_distribution.png` have the wrong y-axis: they currently show counts, but should show total audio duration per duration bin. Need fix analysis script and regenerate plots/report.
- Fixed `analysis/recipe/analyze_jellycat_language_stats.py`: duration distribution bins now accumulate total duration seconds in addition to counts; duration PNGs plot total hours and bar percentages are duration percentages. Markdown duration tables now include Count, Count percent, Hours, and Duration percent.
- Smoke-tested the analysis script on 1,000 records per language under `/tmp/jellycat_analysis_duration_hours_smoke`; generated valid PNGs and report.
- Re-ran full ZH/EN analysis under `analysis/`; regenerated `ZH_duration_distribution.png`, `EN_duration_distribution.png`, `ZH_duration_gt30_distribution.png`, `EN_duration_gt30_distribution.png`, and `jellycat_language_stats.md`.
- Verified all 8 analysis PNGs with Pillow. Report spot-check confirms duration tables now show Hours and Duration percent.
- Stop hook requested continuation after the duration-plot fix. Next remaining implementation item is the versioned stage7-before Lhotse `MonoCut` rewrite path.
- Added `Jellycat/prepare_data/rewrite_jellycat_lhotse_cuts_vad_policy.py`, a generic stage7-before Lhotse `MonoCut` rewrite script. It reads parent reject JSONLs, VAD split maps, optional ASR-backfilled child segment JSONLs, and one or more input cut JSONL/JSONL.GZ files, then writes versioned outputs under `--output-dir`.
- Syntax-checked the new stage7 rewrite script.
- Smoke-tested the stage7 rewrite script on the first 6 ZH raw-cut rows using the existing VAD/backfill smoke artifacts. One parent was replaced by 5 child cuts; output had 10 rows. Verified the first child cut has consistent cut id, nested recording/supervision ids, duration, child FLAC source, ASR text, and raw-cut/VAD metadata in supervision custom.
- Updated `analysis/jellycat_vad_asr_backfill_workflow.md`, `analysis/jellycat_vad_cleanup_plan.md`, and `findings.md` with the stage7 raw-cut rewrite workflow and smoke result.
- Verified `rewrite_jellycat_lhotse_cuts_vad_policy.py --help`.
- Final py_compile across the Jellycat analysis/VAD/audio-cut/ASR-backfill/stage7-rewrite/context/prepare script set passed.
- Marked Phase 4 and Phase 8 complete in `task_plan.md` for the current design/script/smoke-test scope. Remaining work is production launch/promotion, not missing local script implementation.
- User changed the active production requirement: do not do VAD or ASR relabeling; do not touch stage0-6 outputs. Directly reject all audio with duration >=45s, write a non-colliding reject JSONL, remove those long FLACs from Jellycat audio directories, keep ids unchanged, then add prefix/suffix context fields to total segment JSONL and podcast-level JSONLs. Context selection must not skip over rejected audio: if the nearest prefix/suffix item is rejected, the context field should be null.
- Added `Jellycat/prepare_data/apply_jellycat_duration45_reject_context.py`, a direct duration>=45s reject/context application script.
- Syntax-checked the new script and ran a 3-podcast ZH dry-run; reject-aware context nulling for rejected nearest neighbors was observed.
- Ran full ZH/EN dry-runs from podcast JSONLs. ZH: 26,697,838 records, 2,980 rejects, 153.68h rejected, prefix/suffix null due to reject 2,470/2,897. EN: 25,066,601 records, 296 rejects, 42.39h rejected, prefix/suffix null due to reject 273/283.
- Added `analysis/jellycat_vad_asr_backfill_workflow.md` defining split map, child reject, annotation JSONL, raw-source cutting, VibeVoice ASR, backfill, and runtime estimates.
- Updated `analysis/jellycat_vad_cleanup_plan.md` so it no longer conflicts with the latest raw-source child cutting requirement.
- Stop hook reported `0/0 phases done` because `task_plan.md` used a Markdown table that the planning checker does not count as `### Phase` sections.
- Re-read `task_plan.md`, `progress.md`, the planning checker, the planning template, and `Jellycat/prepare_data/apply_jellycat_duration45_reject_context.py`.
- Updated `task_plan.md` with checker-compatible `### Phase` sections for the latest active duration>=45s production policy.
- Current active phase is Phase 3: production apply decision. It is intentionally blocked on explicit user approval because applying with `--delete-audio` deletes FLAC files and replacing manifests modifies production data.
- User redirected the active work to dataset workspace cleanup, moving VAD/ASR scripts to `backup_scripts`, keeping old planning files in place, committing, and pushing.
- Cleanup task planning now lives under `clean/` and is complete.
- Committed and pushed dataset cleanup changes to `origin/main`:
  - `0c257b4 Organize Jellycat dataset cleanup`
  - `1615b9e Mark dataset cleanup complete`
- Stop hook then reported `2/5 phases done` because this old root `task_plan.md` still described deferred production apply phases.
- Re-read `task_plan.md` and `clean/task_plan.md`; production apply remains intentionally not executed because it would replace manifests and may delete FLACs.
- Updated this root `task_plan.md` to close the production-apply phases as deferred, not executed, so the old planning file no longer presents destructive production apply as in-progress work.
